FROM debian:bookworm
RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y sudo openssh-server
COPY ./src/hosts/vultr/install.sh /install.sh
RUN chmod +x ./install.sh
CMD ["./install.sh", "--docker"]
