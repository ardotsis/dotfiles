FROM debian:bookworm
RUN apt-get update && \
    apt-get upgrade -y && \
    apt-get install -y sudo openssh-server ca-certificates
COPY ./dotfiles /dotfiles
COPY ./install.sh /install.sh
RUN chmod +x ./install.sh
CMD ["./install.sh", "vultr"]
